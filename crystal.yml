name: project
type: generator
version: 0.1.0
description: 'project generator for [Crystal](http://crystal.sh)'

dependencies:
  generators:
    authors:
      pass: true
      path: .
      version: latest
    mit-license:
      pass: true
      path: .
      version: latest
    readme:
      pass: true
      path: .
      version: latest